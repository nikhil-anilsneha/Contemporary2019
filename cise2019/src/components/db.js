/* const mySql = require('mysql')

const pool = mySql.createPool({
    connectionLimit: 10,
    host: '127.0.0.1',
    port: '3306',
    user: 'root',
    password: 'cisezawlintun2019',
    database: 'CISE2019'
});

module.exports = pool;
*/
